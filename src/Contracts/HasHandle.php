<?php

declare(strict_types=1);

namespace WireNinja\Accelerator\Contracts;

/**
 * Marker interface for action classes that expose a public handle() method.
 *
 * Each implementing class defines its own handle() signature.
 */
interface HasHandle {}
